# Backend environment variables (TEMPLATE)
#
# IMPORTANT:
# - Copy this file to `backend/.env` for local development.
# - Do NOT commit your real `backend/.env` file (it contains secrets).
#
# Local MongoDB (via Docker Compose):
# MONGODB_URI=mongodb://localhost:27017
#
# MongoDB Atlas (hosted):
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-host>/<db>?retryWrites=true&w=majority

# The MongoDB connection string. Required in production.
MONGODB_URI=mongodb://localhost:27017

# Database name to use inside MongoDB.
MONGODB_DB_NAME=gcobscura

# Express server port
PORT=3000


