# Images not showing in SSR mode

When deployed on the server, the images which should be being served from the /photos folder are not being served. What changes should be made to make sure that Angular can show them in SSR mode? 